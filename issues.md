ROI and extract_along_arc need to fix for unique coordinates after transformation.
if returning list instead return ndarray.

download_from_url module renamed as net , use proper error handling.
running_difference is more elegant than difference_array.

Create App.py a streamlit App for easy access.